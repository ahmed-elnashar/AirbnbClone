import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  markerContainer: {
    backgroundColor: "#fff",
    padding: 5,
    borderRadius: 20,
    borderWidth: 1,
    borderColor: "gray",
  },
  markerText: {
    fontWeight: "bold",
  },
  isSelected: {
    backgroundColor: "#000",
  },
  isSelectedText: {
    color: "#fff",
  },
});

export default styles;
